/// <reference types="@altv/types-client" />
/// <reference types="@altv/types-client" />

import * as alt from 'alt';

export function setCloth(player, arg) {
    // const dlc = alt.hash("mp_m_blifeeup");
    // player.setClothes (arg[0], arg[1], arg[2], arg[3], dlc);
    // chat.send(player, `Component: ${arg[0]}; drawable: ${arg[1]}; texture: ${arg[2]}; palette: ${arg[3]}; dlc: ${dlc}`);
}